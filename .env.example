# GitHub token for GET /events and PR body/files. Required for github:events, github:pull, Connect, and reason.
GITHUB_TOKEN=

# Optional. Max opened PRs Connect produces per 30s sweep (default 1).
# CONNECT_BATCH_LIMIT=1

# Optional. Reason file budget (defaults 20 files, 4000 chars per patch).
# REASON_MAX_NUMBER_FILES=20
# REASON_MAX_FILE_PATCH_SIZE=4000

# Optional. Re-run classify once if confidence is below this (default 0.6).
# CONFIDENCE_THRESHOLD=0.6

# Optional. Ollama URL and model.
# OLLAMA_URL=http://ollama:11434
# OLLAMA_MODEL=llama3:8b

