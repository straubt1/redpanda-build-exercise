# Repair the classification JSON

The previous reply was not valid output. Produce a corrected classification from the same Summary and Input already in this prompt.

## How to repair

- Emit a single JSON object with category, confidence, and rationale.
- category must be one of: security, feature, refactor, docs, dependency-bump, unknown.
- confidence is a number from 0 to 1. Score it from the evidence (patches, competing labels, truncation). A low score is valid; do not inflate it.
- Ground the label in Summary, Body, and Changed Files, not the title alone.
- Title may be an empty string. Body may be empty or truncated. A file may have no patch, or a `[truncated]` patch; still use filename, status, and counts.

## Do not

- Do not make network or API calls. Do not fetch GitHub, clone a repository, follow links, browse the web, or use tools. Rely only on this prompt.
- Do not invent files, diffs, or behavior that are not in the evidence.
- Do not classify from the title alone, or when the title contradicts body and files.
- Do not invent a category outside the allowed list.
- Do not wrap the JSON in markdown fences or add commentary.

## Output

Reply with JSON only: no markdown fences, no commentary.
{"category":"...","confidence":0.0,"rationale":"..."}
